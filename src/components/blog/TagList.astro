---
interface Props {
  tags: string[];
  size?: 'sm' | 'md' | 'lg';
  clickable?: boolean;
}

const { tags, size = 'md', clickable = true } = Astro.props;

const sizeClass = {
  sm: 'text-[10px] px-2 py-0.5',
  md: 'text-xs px-3 py-1',
  lg: 'text-sm px-4 py-1.5',
}[size];
---

{tags.length > 0 && (
  <div class="flex flex-wrap gap-2">
    {tags.map((tag) =>
      clickable ? (
        <a
          href={`/tags/${tag}/`}
          class={`${sizeClass} rounded-full bg-primary-container/10 text-primary-container font-label uppercase tracking-wider border border-primary-container/20 hover:bg-primary-container/20 transition-colors no-underline`}
        >
          #{tag}
        </a>
      ) : (
        <span class={`${sizeClass} rounded-full bg-surface-container text-outline font-label uppercase tracking-wider border border-outline-variant/30`}>
          #{tag}
        </span>
      )
    )}
  </div>
)}
